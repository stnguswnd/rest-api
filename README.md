# REST API 

- 리소스 생성 
  - POST / Create / 
    - POST `/todos`
- 리소스 조회
  - GET / READ
    - GET `/todos`
    - GET `/todos/:id`
- 리소스 수정 
  - PUT(전체) / UPDATE
    - PUT `/todos/:id`
  - PATCH(부분) / UPDATE <- 패치는 굳이 안쓰기로 한다. 
- 리소스 삭제 
  - DELETE / Delete

